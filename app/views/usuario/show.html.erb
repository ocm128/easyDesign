<h2>Usuario: <%= @usuario.username %></h2>
<% if usuario_signed_in? and current_usuario.can_follow?(@usuario.id)  %>
  <button class="btn btn-info" id="follow_btn" data-friend="<%= @usuario.id %>">
  <i class="fa fa-user"></i> Follow</button>
<% end %>