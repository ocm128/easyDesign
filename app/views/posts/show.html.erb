<p id="notice"><%= notice %></p>


<div class="jumbotron">
  <h1><%= @post.titulo %></h1>
  <div>
    <%= @post.contenido %>
  </div>
  <% if usuario_signed_in? %>

     <!-- la varible payment: es la que se pasa como parametro al form_for del form.html.erb
      y crea un nuevo pago vacio. La variable post: es la misma desde la que se está haciendo
      el render del parcial form.html.erb y su valor es el del hidden_field post.id -->
    <%= render "payments/form", payment: Payment.new, post: @post  %>
  <% end %>
</div>

<div class="text-right">
  <% if @post.usuario and usuario_signed_in? and @post.usuario.id == current_usuario.id %>

      <%= link_to 'Edit', edit_post_path(@post) %> |
      <%= link_to 'Eliminar', @post, method: :delete,  data: { confirm: "¿Seguro que quieres borrarlo?"} %> |
   <% end %>
    <%= link_to 'Back', posts_path %>
</div>



